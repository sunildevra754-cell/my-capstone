# Task Manager — AI-Assisted Development

## Prompts used
1. "In src/components/TaskManager.jsx, create a task management app with: 
   an input field to add a new task, a list showing all tasks with a 
   checkbox to mark complete/incomplete, a delete button on each task, 
   completed tasks show with strikethrough text. Use functional component 
   with useState, no external libraries."
2. "Yes, wire it into App.jsx"

## How AI helped
AI generated the complete component in one pass — state management with 
useState, add/toggle/delete handlers, and strikethrough styling for 
completed tasks. It also matched the project's existing CSS variables 
automatically and wired the component into App.jsx without needing 
separate instructions on where to place it.

## Manual improvements/corrections
The generated code worked correctly on first run — adding, completing, 
and deleting tasks all functioned as expected with no manual fixes needed.